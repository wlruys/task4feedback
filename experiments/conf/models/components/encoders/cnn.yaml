defaults:
  - /models/components/shared/activation/leaky_relu@activation
  - /models/components/shared/initialization/orthogonal@initialization


_target_: task4feedback.ml.models.cnn.DilationEncoder
hidden_channels: 16
width: null  # Injected from graph config at runtime
length: null  # Injected from graph config at runtime
minimum_resolution: 4
debug: false