name: critic
inputs:
  - [observation]

nodes:
  encode:
    _target_: task4feedback.ml.nodes.ModuleNode
    module: ${....layers.state}

  value:
    _target_: task4feedback.ml.nodes.ModuleNode
    module: ${....layers.critic}

outputs:
  - [state_value]
