defaults:
  - 4x4x16_static.yaml
  - feature: candidate_coordinate
  - network: vector_baseline
  - _self_

algorithm:
  rollout_steps: 128
  minibatch_size: 128

feature:
  observer:
    grid_override: False
    n_candidates: 32
    version: E
  normalization:
    warmup: 20
  add_device_load: True

network:
  layers:
    state:
      hidden_channels: [16, 16]
    actor:
      hidden_channels: 16
    critic:
      hidden_channels: 128
  critic:
    add_progress: True

wandb:
  name: MLP-A_IEFT
