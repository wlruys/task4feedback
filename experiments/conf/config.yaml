defaults:
  - _self_
  - graph: static_jacobi
  - reward: ieft
  - system: uniform
  - feature: gnn_default
  - network: gnn_baseline
  - algorithm: ppo
  - runtime: batch
  - noise: none  
  - wandb: default
  - logging: default
  - lr_scheduler: none 
  - optimizer: adamw
  - eval: default

hydra:
  callbacks:
    git_info:
      _target_: train.GitInfo
seed: 532
deterministic_torch: true