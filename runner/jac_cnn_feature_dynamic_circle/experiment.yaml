cli_base: "python experiments/train.py --config-name 8x8x128_dynamic_circle_cnn.yaml"

packs:
  - name: wbconfig
    options:
      - wandb:
          project: stencil_cnn_feature_dynamic_circle
        tag: "stencil-CNN-Feature"
  - name: collections
    options:
      - algorithm:
          num_collections: 40000
        tag: "40k"
  - name: entropy
    options:
      - algorithm:
          ent_coef: 0.00025
        tag: "e0.00025"
  - name: rollout
    options:
      - algorithm:
          rollout_steps: 32
          minibatch_size: 32
        tag: "mb32-r32"
  - name: size
    options:
      - graph:
          config:
            n: 8
            steps: 256
        tag: "b8"
  - name: architecture
    options:
      - feature:
          observer:
            version: "A"
        tag: "Top-Pred-Device"
      - feature:
          observer:
            version: "B"
        tag: "Top-Pred-Device-Residency"
      - feature:
          observer:
            version: "C"
        tag: "Top-Pred-Device-Residency-Coord"
  - name: reward
    options:
      - reward:
          pbrs: True
          sparse_reward_scale: 10
          dense_reward_scale: 10
        tag: "pbrs"
  - name: block_time
    options:
      - graph:
          config:
            r_interior: 1
            r_boundary: 0.1
          tag: "1:1:0.1"
      - graph:
          config:
            r_interior: 10
            r_boundary: 0.1
          tag: "1:10:0.1"
      - graph:
          config:
            r_interior: 1
            r_boundary: 1
          tag: "1:1:1"
  - name: seed
    options:
      - seed: 42
        tag: "seed42"
      - seed: 256
        tag: "seed256"
      - seed: 1024
        tag: "seed1024"
      - seed: 2024
        tag: "seed2024"
